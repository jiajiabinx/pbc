# Required: Anthropic API key for the LLM agent
ANTHROPIC_API_KEY=sk-ant-...

# PostgreSQL connection (auto-injected by Railway)
# If not set, falls back to SQLite at data/pbc.db
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Optional: Override default port (Railway sets PORT automatically)
# PORT=8501
